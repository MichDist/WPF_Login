<Page x:Class="WPF_Login.EntryTypeOverview"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF_Login"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EntryTypeOverview">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" Grid.Column="0">
            <Button Name="BtnDiary" Content="Diary" Margin="20" Height="120" Width="120" Click="BtnDiary_Click" />
            <Button Name="BtbNote" Content="Note" Margin="20" Height="120" Width="120" />
            <Button Content="Dream" Margin="20" Height="120" Width="120" />
            <Button Content="General" Margin="20" Height="120" Width="120" />
        </WrapPanel>
        
        <TextBlock Text="Entry Types Overview" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="32"/>

        <Frame x:Name="EntryFrame" Grid.Column="0" Grid.Row="0" NavigationUIVisibility="Hidden"/>
    </Grid>
</Page>
